---
- name: Verify the Solo VPS pinned Grafana Alloy host tooling baseline
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: Verify pinned Grafana Alloy host tooling
      ansible.builtin.import_role:
        name: observability
        tasks_from: verify-tooling.yml

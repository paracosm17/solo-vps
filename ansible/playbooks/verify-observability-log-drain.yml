---
- name: Verify the Solo VPS Coolify application-log drain
  hosts: all
  gather_facts: false
  become: true
  tasks:
    - name: Run the Coolify application-log drain verifier
      ansible.builtin.import_role:
        name: observability
        tasks_from: verify-log-drain

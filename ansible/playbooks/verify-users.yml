---
- name: Verify Solo VPS administrative identity baseline
  hosts: all
  gather_facts: false
  become: true

  tasks:
    - name: Run users role verification tasks
      ansible.builtin.import_role:
        name: users
        tasks_from: verify

---
- name: Verify the Solo VPS repository-managed retained application-log runtime
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: Verify the reviewed retained-log runtime
      ansible.builtin.import_role:
        name: observability
        tasks_from: verify-runtime.yml

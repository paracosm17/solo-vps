---
all:
  hosts:
    solo_vps_example:
      # TEST-NET-3 documentation address. Replace it in a local inventory.
      ansible_host: 203.0.113.10
      # Replace with the provider's initial SSH user when required.
      ansible_user: root

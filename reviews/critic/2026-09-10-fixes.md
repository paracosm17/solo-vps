# Pre-alpha fixes following the 2026-09-09 review

This is a source-change report, not clean-VPS or production evidence. The original review remains a record of the preceding revision.

## Changes and remaining acceptance

| Finding | Implemented response | Still required |
| --- | --- | --- |
| CRIT-021 platform rerun | Let the Coolify playbook select first-install readiness by marker state; verify/reconcile completed installs | Real second platform run |
| CRIT-022 apply/admin handoff | Preserve managed admin inventory on resume, skip bootstrap key preparation, elevate provider handoff with resolved arguments; document one root bootstrap path | Linux provider/admin transition and retained-session replay |
| CRIT-023 interrupted install | Protocol-2 marker, atomic missing-secret initialization, reuse existing/normalized localhost key, refuse missing committed identities | Interrupt at artifact download, secret setup, key setup and container start; prove unchanged credentials |
| CRIT-024 domain guard | Consumer workflow explicitly permits the public domain while retaining internal-port/no-host-mapping restrictions | Hosted domain deployment |
| CRIT-025 first image/CI | Standalone starter; first publication without CD; pinned-version Docker Image fields; production credential setup; version/ENV/log exercise | First image followed by two successful push-deploys |
| CRIT-026 stale workflow | Verify current main through GitHub API inside the serialized deploy job; fail closed on API uncertainty | Delayed older workflow after a newer release |
| CRIT-027 verification | Check activated SSH and Coolify phases; reject interrupted/unmanaged platform state; fail on failed systemd units or low free disk | Stop/unhealthy Coolify, SSH drift and reboot checks on Linux |
| CRIT-028 off-site update dependency | Automatically create a private local DB/config/SSH checkpoint before the supported Coolify upgrade; no restic/S3 prerequisite | Real dump, disposable upgrade and restoration exercise; local copy does not survive VPS loss |
| CRIT-029 update scope | Document exact supported transition, local checkpoint and forward resume; keep generic update as a plan | General source/package self-updater remains out of this pre-alpha implementation; no automatic local-checkpoint restore |
| CRIT-030 sudo confirmation | Set restore confirmation after sudo environment filtering | Optional controlled replacement-host restore |
| CRIT-031 runtime tooling | Install/check minimal Ansible runtime separately; reuse installed prerequisites; remove full QA from handoff | Fresh runtime-only controller and read-only checkout replay |
| CRIT-032 capacity/health | Document resources before rental; enforce fresh-platform capacity before host changes; verify failed units and free space | External outage alert remains an optional integration exercise |
| CRIT-033 repository CI | Run source/docs CI on main and master; preserve PR/manual source checks | Publish upstream, configure security reporting and create a validated immutable release |
| CRIT-034 evidence/complexity | Replace the split first-app journey; distinguish optional acceptance and historical integration evidence; narrow two test fixtures to source inputs | Literal user-path replay remains the primary acceptance criterion |

Additional deployment guard: require a healthy previous application before automatic mutation. A candidate start/status timeout or unknown outcome does not launch an overlapping rollback. A confirmed failed candidate retains image rollback. This is not a database rollback or proof that manual runtime drift cannot exist.

## Verification

- Portable regression suite: 150 tests, 145 passed and 5 Linux/Ansible-dependent phase tests skipped.
- Sample HTTP behavior: 5 tests passed; the generated standalone app also executes its bundled suite in the starter test.
- A separate onboarding test module cannot import Linux `pwd` on Windows. Its static source validator passes; the Linux onboarding tests remain pending.
- Strict English/Russian MkDocs build, YAML lint, Python syntax/error checks, shell syntax, source contract checks and public-data hygiene are checked locally.
- No Linux/WSL controller is installed in this environment. Real Ansible syntax/lint/runtime, Docker images, hosted CI, VPS mutation, external alert and restore exercises are not claimed.
- No private installation values, runtime dumps, local tool dependencies or generated site output are intended for Git.

## Next acceptance run

On a Linux controller run `make qa-tools`, `make validate` and `make qa-static`. Then follow `docs/quick-start.md` and `docs/operations/first-app.md` literally on a disposable Ubuntu 24.04 VPS at the reviewed source revision. Test reruns, interruptions, reboot, health failures, two push-deploys, stale workflows and ENV changes. Preserve private evidence outside the checkout. Exercise the local upgrade checkpoint and forward-resume path separately before making upgrade/recovery readiness claims.
